{% extends 'base.html.twig' %}

{% block body %}
    <h2>Create a wish</h2>

    {{ form_start(wishForm) }}
    {{ form_widget(wishForm) }}
    <button>Add !</button>
    {{ form_end(wishForm) }}
{% endblock %}

